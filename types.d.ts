// types and interfaces
export type Course = {
  studiengang: string;
  kuerzel: string;
  deadline: string;
};

export type Ticket = {
  ticketName: string;
  ticketStart: Date;
  ticketEnd: Date;
  ticketCategory: string;
};
